# CIG Version Information
# Machine-readable version data for the Code Implementation Guide system

# Git-based versioning - use `git describe --tags --always` for current version
# Format: {tag}-{commits-since-tag}-g{commit-hash}
# Example: v0.1.1-5-gcea1c19
git-based-versioning: true

# CIG project schema matches git version for consistency
schema-version: "matches-git"