# CIG Version Information
# Machine-readable version data for the Code Implementation Guide system

# Current version of the cig-project.json schema and CIG system
cig-project: "v1.0.0"

# Git version - should remain in sync with git tags
git: "v0.1.0"